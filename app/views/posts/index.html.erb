<% if @posts.present? %>
  <%= render 'loop_posts', posts: @posts %>
<% elsif @users && params['keyword'] != '' %>
  <% if @users.present? %>
    <% @users.each do |user| %>
      <%= link_to user.name, user_post_path(user) %>
    <% end %>
  <% end %>
<% end %>
